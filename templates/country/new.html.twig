{% extends "#{theme.getParameter('KT_THEME_LAYOUT_DIR')}/_default.html.twig" %}

{% block page_title %}New Country{% endblock %}

{% block content %}
    <h1>Create new Country</h1>

    {{ include('country/_form.html.twig') }}

    <a href="{{ path('admin_country_index') }}">back to list</a>
{% endblock %}

