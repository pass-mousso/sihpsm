{% extends "#{theme.getParameter('KT_THEME_LAYOUT_DIR')}/_default.html.twig" %}

{% block page_title %}Edit Section{% endblock %}

{% block content %}
    <h1>Edit Section</h1>

    {{ include('section_menu/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('admin_section_menu_index') }}">back to list</a>

    {{ include('section_menu/_delete_form.html.twig') }}
{% endblock %}
