{% extends 'base.html.twig' %}

{% block title %}Section{% endblock %}

{% block body %}
    <h1>Section</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ section.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ section.title }}</td>
            </tr>
            <tr>
                <th>Icon</th>
                <td>{{ section.icon }}</td>
            </tr>
            <tr>
                <th>Section_order</th>
                <td>{{ section.sectionOrder }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_section_menu_index') }}">back to list</a>

    <a href="{{ path('app_section_menu_edit', {'id': section.id}) }}">edit</a>

    {{ include('section_menu/_delete_form.html.twig') }}
{% endblock %}
