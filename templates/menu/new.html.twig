{% extends "#{theme.getParameter('KT_THEME_LAYOUT_DIR')}/_default.html.twig" %}

{% block page_title %}New Menu{% endblock %}

{% block content %}
    <h1>Create new Menu</h1>

    {{ include('menu/_form.html.twig') }}

    <a href="{{ path('admin_menu_index') }}">back to list</a>
{% endblock %}
