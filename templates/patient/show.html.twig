{% extends "#{theme.getParameter('KT_THEME_LAYOUT_DIR')}/_default.html.twig" %}

{% block page_title %}Patient Profile{% endblock %}

{% block content %}
    <div class="tab-content" id="myTabContent">
        <!--begin::Navbar-->
        {% include('patient/show/profile_navbar.html.twig') %}
        <!--end::Navbar-->
        <div class="card mb-5 mb-xl-10 tab-pane fade show active" id="kt_profile_details_view" role="tabpanel">
            <!--begin::Card header-->
            <div class="card-header cursor-pointer">
                <!--begin::Card title-->
                <div class="card-title m-0">
                    <h3 class="fw-bolder m-0">Détails de mon profile</h3>
                </div>
                <!--end::Card title-->
                <!--begin::Action-->
                <a href="{{ path('admin_patient_edit', {id:patient.id}) }}" class="btn btn-primary align-self-center">Modifier Profile
                </a>
                <!--end::Action-->
            </div>
            <!--begin::Card header-->
            <!--begin::Card body-->
            {{ include('/patient/show/_informations.html.twig') }}
            <!--end::Card body-->
            {# <div class="card-header cursor-pointer">
            <!--begin::Card title-->
            <div class="card-title m-0">
                <h3 class="fw-bolder m-0">Paramètres de connexion</h3>
            </div>
        </div>
        {{ include('/utilisateur/_security_form.html.twig') }} #}
        </div>
        <div class="card mb-5 mb-xl-10 tab-pane fade" id="kt_user_view_overview_security" role="tabpanel">
            <!--end::Card body-->
            <div class="card-header cursor-pointer">
                <!--begin::Card title-->
                <div class="card-title m-0">
                    <h3 class="fw-bolder m-0">Paramètres de connexion</h3>
                </div>
            </div>
            {# {{ include('/utilisateur/_security_form.html.twig') }} #}
        </div>
    </div>
{% endblock %}
