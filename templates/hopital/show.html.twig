{% extends 'base.html.twig' %}

{% block title %}Hopital{% endblock %}

{% block body %}
    <h1>Hopital</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hopital.id }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ hopital.createdAt ? hopital.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ hopital.updatedAt ? hopital.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_hopital_index') }}">back to list</a>

    <a href="{{ path('app_hopital_edit', {'id': hopital.id}) }}">edit</a>

    {{ include('hopital/_delete_form.html.twig') }}
{% endblock %}
