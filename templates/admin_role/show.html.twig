{% extends 'base.html.twig' %}

{% block title %}AdminRole{% endblock %}

{% block body %}
    <h1>AdminRole</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ admin_role.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ admin_role.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ admin_role.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_role_index') }}">back to list</a>

    <a href="{{ path('app_admin_role_edit', {'id': admin_role.id}) }}">edit</a>

    {{ include('admin_role/_delete_form.html.twig') }}
{% endblock %}
